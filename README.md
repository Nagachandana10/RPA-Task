# RPA-Task
Day 1
1.a Display a Greeting 
1.b Add Two Numbers 
1.c Subtract Two Numbers
1.d Multiply Two Numbers  
1.e Concatenate Two Words 
1.f Create a Simple Full Name Generator
1.g Ask for User's Name  
1.h Pass a Number Between Workflows  
1.i Pass a Name to Another Workflow
1.j Employee Salary Calculation Using Arguments
Day 2
1.if a given number is positive. If yes, display “Positive Number.”
2.if a string is not empty. If true, display “String is not empty.”
3.if today’s date matches a specified date. If yes, display “It’s the same day!”
4.if a number is even or odd. Display “Even” or “Odd” accordingly.
5.if a string starts with “A.” If true, display “Starts with A,” otherwise “Does not start with A.”
6.if an input temperature is above 30°C. If yes, display “Hot,” otherwise display “Cool.”
7.if the length of a given string is greater than 5. If true, display “Long String,” otherwise “Short String.”
8.Check the grade of a student:
 If marks are above 90, display “Grade A.”
 If marks are between 75 and 90, display “Grade B.”
 Otherwise, display “Grade C”
9.Categorize a number:
 If number < 0, display “Negative.”
 If number = 0, display “Zero.”
 If number > 0, display “Positive.”
10.Decide if a year is a leap year:
 If divisible by 4 and not divisible by 100, display “Leap Year.”
 If divisible by 400, display “Leap Year.”
 Otherwise, display “Not a Leap Year.”
11.Create an employee validation workflow using:
 employeeName
 age
 salary
 department
 -	Employee is eligible if:
 age >= 18
 salary > 25000
 department <> "Intern"
 -	If all conditions are satisfied:
 Employee is Eligible
 Otherwise:
 Employee is Not Eligible
 Rating the Employee Performance

